apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kiali-stack.fullname" . }}-test-connection"
  labels:
    {{- include "kiali-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kiali-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
